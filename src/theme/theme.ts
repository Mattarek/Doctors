import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    common: {
      black: "#000",
      white: "#fff",
    },
    primary: {
      main: "#9E303C",
      contrastText: "#ffffff",
    },
    secondary: {
      main: "#7950DE",
      contrastText: "#ffffff",
    },
  },
});
